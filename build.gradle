apply plugin: 'java'
apply plugin: 'eclipse'

sourceSets {
    main {
        java {
            srcDir 'src/'
            srcDir 'gen/'
        }
        resources {
            srcDir 'res/'
        }
    }
}

buildDir = 'out/'

repositories {
    mavenCentral()
}

dependencies { }
